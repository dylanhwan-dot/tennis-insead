NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Only needed to run scripts/seed.mjs. Never used by the app itself.
# Bypasses RLS — get it from Supabase dashboard > Settings > API (labeled
# "service_role" or "secret key"). Never commit a real value here.
SUPABASE_SERVICE_ROLE_KEY=
